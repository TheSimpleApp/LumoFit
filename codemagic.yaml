# Codemagic CI/CD Configuration for FitTravel
# Documentation: https://docs.codemagic.io/yaml-quick-start/building-a-flutter-app/

workflows:
  ios-release:
    name: iOS App Store Release
    max_build_duration: 120
    instance_type: mac_mini_m2
    
    # App Store Connect Integration
    # You need to set up the API key in Codemagic Team Settings > Integrations > App Store Connect
    # Name your key "codemagic" or change the value below to match your key name
    integrations:
      app_store_connect: codemagic
    
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.simple.lumofit
      vars:
        # Set this to your App Store Apple ID (the numeric ID from App Store Connect)
        # You can find this in App Store Connect > App Information > Apple ID
        APP_STORE_APPLE_ID: 0000000000  # Replace with your actual App Store Apple ID
      flutter: stable
      xcode: latest
      cocoapods: default
    
    # Trigger builds on push to main/master branch
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: 'main'
          include: true
          source: true
        - pattern: 'master'
          include: true
          source: true
    
    scripts:
      # Set up code signing using profiles from App Store Connect
      - name: Set up code signing settings on Xcode project
        script: | 
          xcode-project use-profiles

      # Get Flutter dependencies
      - name: Get Flutter packages
        script: | 
          flutter pub get

      # Install CocoaPods dependencies
      - name: Install pods
        script: | 
          find . -name "Podfile" -execdir pod install \;

      # Run Flutter analyze for code quality
      - name: Flutter analyze
        script: | 
          flutter analyze
        ignore_failure: true

      # Run Flutter tests
      - name: Flutter unit tests
        script: | 
          flutter test
        ignore_failure: true

      # Build iOS IPA for App Store
      - name: Flutter build ipa
        script: | 
          flutter build ipa --release \
            --build-name=1.0.0 \
            --build-number=$(($(app-store-connect get-latest-app-store-build-number "$APP_STORE_APPLE_ID") + 1)) \
            --export-options-plist=/Users/builder/export_options.plist
    
    # Collect build artifacts
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - flutter_drive.log
    
    # Publishing configuration
    publishing:
      # Email notifications (optional - add your email addresses)
      email:
        recipients:
          - your-email@example.com  # Replace with your email
        notify:
          success: true
          failure: true
      
      # App Store Connect publishing
      app_store_connect:
        auth: integration
        
        # Submit to TestFlight for beta testing
        submit_to_testflight: true
        
        # Expire previous builds waiting for review
        expire_build_submitted_for_review: true
        
        # Beta tester groups (optional - add your group names)
        # beta_groups:
        #   - Internal Testers
        #   - Beta Testers
        
        # App Store submission (set to true when ready for production)
        submit_to_app_store: false
        
        # Release type: MANUAL, AFTER_APPROVAL, or SCHEDULED
        release_type: MANUAL

