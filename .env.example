# FitTravel Environment Variables
#
# Security Model: All external API keys (Google Places, Gemini, Eventbrite, RunSignup)
# are stored in Supabase Edge Functions environment variables, NOT in the Flutter app.
# The app only needs Supabase credentials to call Edge Functions.
#
# DO NOT commit .env to version control

# Supabase Project URL
# Format: https://your-project-ref.supabase.co
# Found in: Supabase Dashboard > Settings > API
SUPABASE_URL=your_supabase_url_here

# Supabase Anonymous Key (safe to expose in client apps)
# Found in: Supabase Dashboard > Settings > API > Project API keys > anon/public
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================================================
# External API Keys (STORED IN SUPABASE EDGE FUNCTIONS, NOT IN FLUTTER APP)
# ============================================================================
# These are managed in Supabase Dashboard > Edge Functions > Environment Variables:
# - GOOGLE_PLACES_API_KEY (for gyms, restaurants, trails search)
# - GEMINI_API_KEY (for AI Cairo Guide)
# - EVENTBRITE_PRIVATE_TOKEN (for events discovery)
# - RUNSIGNUP_API_KEY (for running events)
# - RUNSIGNUP_API_SECRET (for running events)
# ============================================================================

# Build commands:
# Development (uses fallback Supabase credentials from AppConfig):
#   flutter run

# Production (requires Supabase credentials):
#   flutter build apk \
#     --dart-define=SUPABASE_URL=$SUPABASE_URL \
#     --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY

# iOS Production:
#   flutter build ios \
#     --dart-define=SUPABASE_URL=$SUPABASE_URL \
#     --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY

# TestFlight/App Store:
#   flutter build ipa \
#     --dart-define=SUPABASE_URL=$SUPABASE_URL \
#     --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY \
#     --export-options-plist=ios/ExportOptions.plist
